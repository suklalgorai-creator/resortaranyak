import { NextResponse } from 'next/server';

// GET /api/rooms — List all active rooms
export async function GET() {
  // Will be implemented in Phase 4
  return NextResponse.json({ message: 'Rooms API — Coming in Phase 4' });
}
