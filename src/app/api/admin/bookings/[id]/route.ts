import { NextResponse } from 'next/server';

export async function PATCH() {
  return NextResponse.json({ message: 'Admin booking update API — Coming in Phase 7' });
}

export async function GET() {
  return NextResponse.json({ message: 'Admin booking detail API — Coming in Phase 7' });
}
